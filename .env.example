# Copy to .env and fill in. docker-compose.yml reads this file automatically.

# Optional: only needed if you authenticate with an API key instead of
# interactive OAuth login (`claude` then /login).
ANTHROPIC_API_KEY=

# Git identity used for commits inside the sandbox.
# Take these over from your host with:
#   git config --global user.name   -> GIT_USER_NAME
#   git config --global user.email  -> GIT_USER_EMAIL
GIT_USER_NAME=
GIT_USER_EMAIL=

# Which profile to use by default — it carries the skills / plugins / MCP
# servers *and* the directory to hand to the sandbox, recorded in
# profiles/<name>/workspace by `./oglimmer.sh new`. See profiles/README.md.
#
# Deliberately no WORKSPACE_DIR here: the workspace belongs to the profile, so
# there is one source of truth and the two can't drift apart. Override a single
# run with `./oglimmer.sh -w /path/to/repo run` instead.
CLAUDE_PROFILE=default

# Secrets referenced as ${VAR} from a profile's mcp.json. Keep them here rather
# than in mcp.json itself, and add a passthrough in docker-compose.yml's
# environment: block for any you add.
GRAFANA_URL=
GRAFANA_SERVICE_ACCOUNT_TOKEN=

# GitHub (gh) / GitLab (glab) CLI auth. Leave these blank for the common case:
# the wrapper resolves the token from your host's own `gh` / `glab` at run time
# and forwards it, so both CLIs are logged in inside the sandbox automatically.
# Set them only to override that — a self-hosted instance, or a scoped token you
# want the sandbox to use instead of your personal one. A value here wins over
# the auto-resolved host token. HOST defaults to github.com / gitlab.com.
GH_TOKEN=
GH_HOST=
GITLAB_TOKEN=
GITLAB_HOST=
